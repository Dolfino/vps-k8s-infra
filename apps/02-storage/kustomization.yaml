apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: storage

resources:
  - postgres.yaml
  - pgbouncer.yaml
  - redis.yaml
  - minio.yaml

generators:
  - secret-generator.yaml
