apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: business

resources:
  - configmap.yaml
  - pvc.yaml
  - migration-job.yaml
  - deployment.yaml
  - service.yaml

generators:
  - secret-generator.yaml

commonLabels:
  app.kubernetes.io/part-of: evolution-platform
  app.kubernetes.io/managed-by: argocd
