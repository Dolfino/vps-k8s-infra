apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

configMapGenerator:
  - name: grafana-dashboards
    files:
      - sre-cluster-overview.json
      - evolution-api-overview.json
      - logs-and-events.json

generatorOptions:
  disableNameSuffixHash: true
